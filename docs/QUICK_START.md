# ⚡ Быстрый старт: Cash Flow Planner

## 🎯 Цель

За **15 минут** вы:
1. Установите проект
2. Настроите окружение
3. Запустите приложение
4. Увидите, что нужно сделать

---

## 📦 Шаг 1: Установка (5 минут)

```bash
# Убедитесь, что вы в папке проекта
cd /Users/mo/claude_code/fcf_planner

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

**Ожидаемый результат:**
```
Successfully installed streamlit-1.32.0 pandas-2.2.0 openai-1.51.2 ...
```

---

## 🔑 Шаг 2: Настройка API ключа (2 минуты)

```bash
# Создайте .env файл
cp .env.example .env

# Откройте .env в редакторе
nano .env  # или любой другой редактор
```

**Отредактируйте файл `.env`:**
```bash
# Вставьте свой реальный ключ
OPENAI_API_KEY=sk-proj-your-actual-key-here

# Остальное можно оставить по умолчанию
DB_URL=sqlite:///cashflow.db
FORECAST_LOOKBACK_MONTHS=3
HIST_COL_DATE=date
HIST_COL_CATEGORY=category
HIST_COL_AMOUNT=amount
```

**Где взять API ключ?**
1. Зайдите на https://platform.openai.com/api-keys
2. Создайте новый ключ
3. Скопируйте и вставьте в `.env`

---

## 🚀 Шаг 3: Запуск приложения (3 минуты)

```bash
# Запустите Streamlit
streamlit run app.py
```

**Ожидаемый результат:**
```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.1.x:8501
```

Откройте браузер по адресу: **http://localhost:8501**

---

## 📊 Шаг 4: Загрузка тестовых данных (2 минуты)

В открывшемся приложении:

1. Нажмите **"Browse files"**
2. Выберите файл: `data/sample_cashflow.csv`
3. Увидите статистику:
   - Записей: 27
   - Категорий: 8
   - Месяцев: 3
   - Сумма: ~5,000,000

4. Нажмите **"🔮 Рассчитать прогноз"**

---

## ❌ Шаг 5: Ожидаемая ошибка (3 минуты)

Вы увидите ошибку:
```
❌ Модуль agents/forecast_agent.py не найден или не содержит функцию build_cashflow_forecast().
Выполните ЗАДАНИЕ 1!
```

**Это нормально!** 🎉

Это значит, что:
- ✅ Приложение работает
- ✅ База данных подключена
- ✅ Загрузка файлов работает
- ❌ Но AI-агент еще не написан — **это ваша задача!**

---

## 📚 Что дальше?

Теперь откройте:

```bash
# Прочитайте первое задание
cat tasks/TASK_01_FORECAST_AGENT.md

# Или откройте в редакторе
code tasks/TASK_01_FORECAST_AGENT.md
```

Следуйте инструкциям в задании — создайте файл `agents/forecast_agent.py`.

---

## 🧪 Как проверить результат?

После выполнения Задания 1:

```bash
# Тест через консоль
python test_forecast.py

# Или через веб-интерфейс
streamlit run app.py
# → загрузите файл → нажмите "Рассчитать прогноз"
```

---

## 🆘 Проблемы?

### Ошибка: "No module named 'streamlit'"
**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: "Invalid API key"
**Решение:**
```bash
# Проверьте .env файл
cat .env

# API ключ должен начинаться с sk-proj- или sk-
# Убедитесь, что нет лишних пробелов
```

### Ошибка: "File not found: data/sample_cashflow.csv"
**Решение:**
```bash
# Убедитесь, что вы в правильной папке
pwd  # должно быть: /Users/mo/claude_code/fcf_planner

# Проверьте наличие файла
ls data/sample_cashflow.csv
```

### Streamlit не открывается
**Решение:**
```bash
# Попробуйте явно указать порт
streamlit run app.py --server.port 8501

# Или другой порт
streamlit run app.py --server.port 8502
```

---

## 📝 Чеклист готовности

Перед началом Задания 1 убедитесь:

- [ ] Виртуальное окружение активировано (`venv` в командной строке)
- [ ] Все зависимости установлены (нет ошибок при `pip install`)
- [ ] `.env` файл создан и содержит реальный API ключ
- [ ] Streamlit запускается без ошибок
- [ ] Тестовый файл загружается и показывает статистику
- [ ] Вы видите ошибку о том, что `forecast_agent.py` не найден

---

## 🎓 Следующие шаги

1. **Прочитайте:** `tasks/TASK_01_FORECAST_AGENT.md`
2. **Создайте:** `agents/forecast_agent.py`
3. **Реализуйте:** 3 функции (см. задание)
4. **Тестируйте:** `python test_forecast.py`
5. **Запускайте:** `streamlit run app.py`

---

## 💡 Полезные команды

```bash
# Запуск приложения
streamlit run app.py

# Запуск тестов
python test_forecast.py

# Проверка структуры проекта
tree -L 2  # или ls -R

# Просмотр логов
streamlit run app.py --logger.level=debug

# Остановка приложения
Ctrl + C
```

---

**Готовы? Начинайте с Задания 1!** 🚀

Файл: `tasks/TASK_01_FORECAST_AGENT.md`
